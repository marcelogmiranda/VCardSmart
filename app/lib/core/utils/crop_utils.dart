import 'dart:io';

class CropUtils {
  CropUtils._();

  static Future<File> cropToSquare(File file) async {
    return file; // Simplified - in production use image_cropper package
  }

  static Future<File> cropToCircle(File file) async {
    return file; // Simplified - in production use image_cropper package
  }
}
